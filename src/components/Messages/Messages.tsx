import {MessageDetails} from "../index";
import MessagesStyled from "./MessagesStyled";

const Messages = () => {
    return (
        <MessagesStyled>
            Messages
            <MessageDetails/>
            <MessageDetails/>
            <MessageDetails/>
        </MessagesStyled>
    );
}

export default Messages
