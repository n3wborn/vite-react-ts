import {MessageDetails} from "../index";
import MessagesStyled from "./MessagesStyled";

const Messages = () => {
    return (
        <MessagesStyled>
            <MessageDetails/>
            <MessageDetails/>
            <MessageDetails/>
            <MessageDetails/>
            <MessageDetails/>
            <MessageDetails/>
        </MessagesStyled>
    );
}

export default Messages
