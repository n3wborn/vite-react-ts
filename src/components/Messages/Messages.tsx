import {MessageDetails} from "../index";

const Messages = () => {
    return (
        <div>
            Messages
            <MessageDetails/>
            <MessageDetails/>
            <MessageDetails/>
        </div>
    );
}

export default Messages
